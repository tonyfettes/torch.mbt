{
  "pre-build": [
    {
      "input": "../bin/generate_tuple/main.mbt",
      "output": "nn_tuple.mbt",
      "command": "cd bin && moon run generate_tuple"
    }
  ],
  "targets": {
    "tensor_add.wasm.mbt": ["wasm"],
    "tensor_add.mbt": ["not", "wasm"],
    "tensor_mul.wasm.mbt": ["wasm"],
    "tensor_mul.mbt": ["not", "wasm"]
  }
}
