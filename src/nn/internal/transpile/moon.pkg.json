{
  "pre-build": [
    {
      "input": "../../../../bin/generate/nn/transpile/tuple/main.mbt",
      "output": "tuple.mbt",
      "command": "(cd bin && moon run generate/nn/transpile/tuple) && moonfmt -w $output"
    }
  ],
  "test-import": [
    "tonyfettes/torch/shape",
    "tonyfettes/torch/distributions",
    "tonyfettes/torch/nn"
  ]
}
